<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aditi.tyrotrojan" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <uses-sdk
            android:minSdkVersion="8"
            android:targetSdkVersion="21" />

        <uses-permission android:name="android.permission.CALL_PHONE" />
        <uses-permission android:name="android.permission.READ_PHONE_STATE" />

        <activity android:name=".HomeScreenActivity"
            android:label="HomeScreenActivity" ></activity>
            <activity android:name=".ShowCafesActivity" android:label="ShowCafesActivity"></activity>
        <activity android:name=".CafeActivity" android:label="cafeActivity"></activity>
        <activity android:name=".Deals_Activity" android:label="@string/title_activity_deals_"></activity>
    </application>

</manifest>
